# WasmBrowser entry: component backend + the same shared app.elisa the native
# entry uses. The backend owns the WIT guest exports, so this file only chooses
# a platform and an application.

include "../../src/platform/wasmbrowser/ui_wasmbrowser.elisa"
include "../../src/widgets/ui_widget.elisa"
include "app.elisa"
